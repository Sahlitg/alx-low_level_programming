#include "main.h"

void _puts(char *str);

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int main(void)
{
	char *str;

	str = "I don not fear computer. I fear the lack of them - Issac Asimov";
	_puts(str);
	return (0);
}
